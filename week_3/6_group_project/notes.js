//Alternate sum
arrayA.forEach(function(value) {sum += value;});

//for loop sum
		for (var i = 0; total = 0; i < array.A length; i++) {
			total += arrayA[i];
		}
		return total;
		
		arrayA = [1,2,3];
		"1+2+3"

//alternate sum
return array.length === 0 ? 0 : eval(arrayA.join('+'));

//examples from median
array = [1, 2, 3, 4]

var middle = array.length / 2; // 2.5

return array[middle - .5]; // array[2.5 - .5] => array[2]

array.length = 4
middle = 2

return ( array[2] + array[1] ) / 2
return (3 + 2) / 2; => 2.5